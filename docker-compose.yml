version: '3.9'
services:
  db:
    image: postgres:16
    container_name: postgres_crud
    environment:
      POSTGRES_DB: cruddb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"

